<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- use android:Widget.ProgressBar.Horizontal as parent for a thick one,
        Base.Widget.AppCompat.ProgressBar.Horizontal for thin-->
    <style name="CustomProgressBarHorizontal" parent="Base.Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:minHeight">10dip</item>
        <item name="android:maxHeight">20dip</item>
    </style>

</resources>
